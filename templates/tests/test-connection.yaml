apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dotnetwebappsdemo.fullname" . }}-test-connection"
  labels:
    {{- include "dotnetwebappsdemo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dotnetwebappsdemo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
